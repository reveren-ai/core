# Pre-push parity gate. Runs the same checks GitHub Actions runs so CI
# failures get caught locally before they hit origin.
#
# To bypass in an emergency: `git push --no-verify` (use sparingly).

pnpm pre-push
